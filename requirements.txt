# 核心依赖
psutil
requests>=2.25.0
pywin32>=227; sys_platform == "win32"
pyyaml>=6.0

# Web GUI
fastapi>=0.109.0
uvicorn>=0.27.0
python-multipart>=0.0.6

# GUI 美化 (保留以防回滚)
ttkbootstrap>=1.10.0

# Yara 规则扫描 (可选，如安装失败可跳过)
yara-python>=4.0.0

# 开发和测试依赖 (可选)
pytest>=6.2.0
pytest-cov>=2.12.0
black>=21.6b0
flake8>=3.9.0
mypy>=0.910
